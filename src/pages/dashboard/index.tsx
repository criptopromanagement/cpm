import { NextPage } from "next/types";
import { Stepper } from "../../components/account/validation/stepper";

const Dashboard: NextPage = () => {

    return(
        <Stepper/>
    )
};

export default Dashboard;